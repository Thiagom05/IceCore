// Tailwind CSS v4 usa configuración CSS-first en src/index.css
// Este archivo se mantiene mínimo por compatibilidad si fuera necesario, 
// pero la configuración principal está en index.css
export default {
    content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
    theme: {
        extend: {},
    },
    plugins: [],
}
