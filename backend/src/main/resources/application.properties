spring.application.name=IceCore
# --- CONEXIÓN A BASE DE DATOS ---
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/icecore_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:ncstis.05}
spring.datasource.driver-class-name=org.postgresql.Driver

# --- CONFIGURACIÓN DE JPA / HIBERNATE ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# --- CONFIGURACIÓN DEL SERVIDOR ---
server.port=${PORT:8080}

# --- MERCADO PAGO ---
mercadopago.access_token=APP_USR-4054318563070908-020201-d2b9e994eea8f9597590cd631806155d-3170245289
